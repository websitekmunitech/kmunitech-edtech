# Copy this to .env.local and fill in your values
VITE_API_BASE_URL=http://localhost:3000

# Supabase (Storage for lesson videos)
# IMPORTANT: use the anon/public key (publishable). Do NOT put service_role key in the frontend.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Name of the Storage bucket that holds lesson videos (must already exist)
VITE_SUPABASE_VIDEO_BUCKET=edtechkmunitek
